client.cmd - odpala client react
server.cmd - odpala server express
database.cmd - odpala dostƒôp do db (do edytowania struktury sql)

refrences - linki do ≈∫r√≥de≈Ç pomocniczych

app-client - folder z projektem strony
app-server - folder z projektem serevera/api w express z dostƒôpem do bazy sqlite3

PORT client - 3000
PORT server - 8080

struktura bd:

uøytkownicy (nick PK, has≈Ço, imie, nazwisko)
create table users(nickname varchar(25) not null, password varchar not null, name varchar(25) not null, surname varchar(25) not null, primary key (nickname));

notatki (tytu≈Ç, priorytet, opis, sale, nick autora, nicki os√≥b dotyczƒÖcych notatki w tym autora, data dodania, deadline)
create table posts(title varchar not null, priority int(1) not null, description varchar, rooms varchar, author_nickname varchar(25) not null, contributors_nicknames varchar not null, date_added datetime not null, deadline datetime not null, foreign key (author_nickname) references users(nickname));

sticky notki (zawartoúÊ, data dodania, priorytet)
create table sticky_notes(content varchar, date_added datetime, priority int(1) not null);

inserty:

insert into users
values ('nickname', 'haslo', 'imie', 'nazwisko');

insert into posts
values ('d≥ugi tytu≥ d≥ugi tytu≥ d≥ugi tytu≥',5,'opis','203','asinatio','nickname,asinatio','11-0502023','24-05-2023');

insert into sticky_notes
values ('jaka≈õ sticky notka', '11-0502023',5);
